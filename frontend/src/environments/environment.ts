// Production build config (used by `npm run build`, i.e. the Vercel deploy).
// Set apiUrl to the Render API URL once the backend is deployed, e.g.
//   apiUrl: 'https://bookshelf-api.onrender.com'
// No trailing slash.
export const environment = {
  production: true,
  apiUrl: 'https://bookshelf-api.onrender.com',
};
